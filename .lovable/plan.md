

# QuarterCell: Перегруппировка полей в expanded view

## Что меняем

Группируем поля по логике использования:
- **Верх**: Поля для команды (статусы, метрики, доп. расходы, комментарий)
- **Низ**: Поле для администратора (базовая стоимость)

## Текущий порядок полей в expanded view

```text
1. Базовая стоимость  ← для админа
2. Доп. расходы       ← для команды
3. Итого
4. Поддержка (toggle)
5. On-Track (toggle)
6. План метрики
7. Факт метрики
8. Комментарий        ← для команды
```

## Новый порядок полей

```text
1. Поддержка (toggle)    ← статусы первыми
2. On-Track (toggle)
3. План метрики          ← метрики
4. Факт метрики
5. Доп. расходы          ← поля команды вместе
6. Комментарий
─────────────────────
7. Базовая стоимость     ← админское поле в конце
8. Итого: X ₽
```

## Визуальный результат

```text
┌─────────────────────────┐
│ [35 ] %  🔧       [▲]   │
├─────────────────────────┤
│ Поддержка: [toggle]     │  ← Статусы
│ On-Track: [toggle]      │
│                         │
│ План: [____]            │  ← Метрики
│ Факт: [____]            │
│                         │
│ Доп. расходы: [___] ₽   │  ← Поля команды
│ Комментарий: [____]     │
│ ─────────────────────   │  ← Разделитель
│ Базовая стоимость       │  ← Админское поле
│ [________] ₽            │
│ Итого: 1.2M ₽           │
└─────────────────────────┘
```

## Технические изменения

### Файл: `src/components/admin/QuarterCell.tsx`

Перестановка секций внутри `CollapsibleContent`:

1. **Режим поддержки** (Support toggle) — без изменений
2. **On-Track toggle** — без изменений  
3. **План метрики** — без изменений
4. **Факт метрики** — без изменений
5. **Доп. расходы** — переносим сюда (из секции стоимости)
6. **Комментарий** — без изменений
7. **Разделитель** — добавляем `<Separator />` или border
8. **Базовая стоимость** — переносим в конец
9. **Итого** — остаётся после базовой стоимости

## Порядок реализации

1. Перенесу секцию "Режим поддержки" и "On-Track" в начало expanded view
2. Оставлю метрики (План/Факт) после статусов
3. Перенесу "Доп. расходы" перед комментарием
4. Добавлю визуальный разделитель
5. Перенесу "Базовая стоимость" и "Итого" в самый низ

